{
  "mcpServers": {
    "google-ads-mcp": {
      "command": "bash",
      "args": ["-c", "set -a; [ -f .env ] && . ./.env; set +a; exec pipx run --spec git+https://github.com/googleads/google-ads-mcp.git google-ads-mcp"]
    },
    "google-analytics-mcp": {
      "command": "bash",
      "args": ["-c", "set -a; [ -f .env ] && . ./.env; set +a; exec pipx run analytics-mcp"]
    }
  }
}
